name: total_pos
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  bloc: ^8.0.3
  flutter_bloc: ^8.0.1
  uuid: ^3.0.6
  get_it: ^7.2.0
  postgres: ^2.4.3
  shared_preferences: ^2.0.13

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  
flutter:

  uses-material-design: true

  assets:
    - lib/app/assets/images/
    - lib/backend/assets/postgres/
